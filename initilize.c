#include "window.h"

int main(void)
{
	/* starts up SDL and creates window */
	bool init(void);

	/* loads media */
	bool loadMedia(void);

	/* frees media and shuts down SDL */
	void xclose(void);

	return (0);
}
