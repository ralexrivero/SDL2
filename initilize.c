#include "window.h"

int main(void)
{
	/* starts up SDL and creates window */
	void init(void);

	/* loads media */
	void loadMedia(void);

	/* frees media and shuts down SDL */
	void _close(void);

	return (0);
}
